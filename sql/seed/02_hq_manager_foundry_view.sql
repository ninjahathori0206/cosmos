/*
  DEPRECATED — assign hq_manager (and all role) permissions in Command Unit → Roles.

  Legacy script retained only as a no-op so old runbooks that reference this path do not fail.
*/
PRINT 'sql/seed/02_hq_manager_foundry_view.sql — skipped (auto-seed disabled; use Command Unit → Roles).';

USE [CosmosERP];
GO
